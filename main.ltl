// ltl-dda-line/main.ltl
//
// DDA / Bresenham line / circle rasterisers.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-dda-line v0.1.0")
    io::println("DDA / Bresenham line / circle rasterisers.")
    0
}
